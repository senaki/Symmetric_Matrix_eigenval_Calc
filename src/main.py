from math import *
import os
import pathlib
# File object
file=[input("dir : "), input("file name : ")]
print("File name :" + file[0] + file[1] + '\n')
# Operation on File
# Open for reading/writing/display content/, Close
#
